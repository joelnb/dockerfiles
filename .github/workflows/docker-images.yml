name: docker-images

on:
  push:
    branches:
      - 'master'
  schedule:
    - cron: '0 2 1 * *'

jobs:
  docker_get_deb:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push Get Deb image (latest)
        id: docker_build_get_deb_latest
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/get-deb:latest
          context: ./get-deb
          file: ./get-deb/Dockerfile
      - name: Image digest
        run: echo ${{ steps.docker_build_get_deb_latest.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/get-deb:latest
          fail-build: false
      - name: Build and push Get Deb image (xenial)
        id: docker_build_get_deb_xenial
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/get-deb:xenial
          context: ./get-deb
          file: ./get-deb/Dockerfile-xenial
      - name: Image digest
        run: echo ${{ steps.docker_build_get_deb_xenial.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/get-deb:xenial
          fail-build: false
      - name: Build and push Get Deb image (bionic)
        id: docker_build_get_deb_bionic
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/get-deb:bionic
          context: ./get-deb
          file: ./get-deb/Dockerfile-bionic
      - name: Image digest
        run: echo ${{ steps.docker_build_get_deb_bionic.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/get-deb:bionic
          fail-build: false
      - name: Build and push Get Deb image (focal)
        id: docker_build_get_deb_focal
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/get-deb:focal
          context: ./get-deb
          file: ./get-deb/Dockerfile-focal
      - name: Image digest
        run: echo ${{ steps.docker_build_get_deb_focal.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/get-deb:focal
          fail-build: false
      - name: Build and push Get Deb image (debian-latest)
        id: docker_build_get_deb_debian_latest
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/get-deb:debian-latest
          context: ./get-deb
          file: ./get-deb/Dockerfile-debian-latest
      - name: Image digest
        run: echo ${{ steps.docker_build_get_deb_debian_latest.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/get-deb:debian-latest
          fail-build: false
      - name: Build and push Get Deb image (bullseye)
        id: docker_build_get_deb_bullseye
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/get-deb:bullseye
          context: ./get-deb
          file: ./get-deb/Dockerfile-bullseye
      - name: Image digest
        run: echo ${{ steps.docker_build_get_deb_bullseye.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/get-deb:bullseye
          fail-build: false
      - name: Build and push Get Deb image (buster)
        id: docker_build_get_deb_buster
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/get-deb:buster
          context: ./get-deb
          file: ./get-deb/Dockerfile-buster
      - name: Image digest
        run: echo ${{ steps.docker_build_get_deb_buster.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/get-deb:buster
          fail-build: false
  docker_gollum:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push Gollum image (nginx)
        id: docker_build_gollum_nginx
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/gollum:nginx
          context: ./gollum/nginx
          file: ./gollum/nginx/Dockerfile
      - name: Image digest
        run: echo ${{ steps.docker_build_gollum_nginx.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/gollum:nginx
          fail-build: false
      - name: Build and push Gollum image (latest)
        id: docker_build_gollum_latest
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/gollum:latest
          context: ./gollum
          file: ./gollum/Dockerfile
      - name: Image digest
        run: echo ${{ steps.docker_build_gollum_latest.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/gollum:latest
          fail-build: false
  docker_ssh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push ssh image (trusty)
        id: docker_build_ssh_trusty
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/ssh:trusty
          context: ./ssh/trusty
          file: ./ssh/trusty/Dockerfile
      - name: Image digest
        run: echo ${{ steps.docker_build_ssh_trusty.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/ssh:trusty
          fail-build: false
      - name: Build and push ssh image (xenial)
        id: docker_build_ssh_xenial
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/ssh:xenial
          context: ./ssh/xenial
          file: ./ssh/xenial/Dockerfile
      - name: Image digest
        run: echo ${{ steps.docker_build_ssh_xenial.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/ssh:xenial
          fail-build: false
      - name: Build and push ssh image (latest)
        id: docker_build_ssh_latest
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: joelnb/ssh:latest
          context: ./ssh
          file: ./ssh/Dockerfile
      - name: Image digest
        run: echo ${{ steps.docker_build_ssh_latest.outputs.digest }}
      - name: Scan image
        uses: anchore/scan-action@v3
        with:
          image: joelnb/ssh:latest
          fail-build: false
